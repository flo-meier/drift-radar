{
  "name": "drift-radar-trigger",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that lets jury members trigger a Drift Radar workflow run without GitHub write access.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.85.0"
  }
}
